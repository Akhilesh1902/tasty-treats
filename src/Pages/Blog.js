import React from "react"

const Blog = () => {
  return (
    <div
      className="BlogContainer"
      style={{
        height: "100vh",
        width: "100vw",
        background: "black",
        display: "grid",
        placeItems: "center",
      }}>
      <h1 style={{ color: "whitesmoke", fontFamily: "var(--pf-font-)" }}>
        Under Construction
      </h1>
    </div>
  )
}

export default Blog
